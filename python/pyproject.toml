[project]
name = "another-me-worker"
version = "1.1.0"
description = "另一个我 — Python NLP Worker（现实主义因子 + TF-IDF 聚类）"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "snownlp>=0.12.3",
    "jieba>=0.42.1",
    "scikit-learn>=1.6.1",
    "numpy>=2.2.4",
]
